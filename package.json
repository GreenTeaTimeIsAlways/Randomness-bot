{
  "name": "discord-verification-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Discord verification and event bot for Cloudflare Workers + Gemini",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
